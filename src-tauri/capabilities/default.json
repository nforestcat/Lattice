{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Allow the main Lattice window to use core APIs and open vault folders.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
